import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
    /* config options here */
    devIndicators: {
        buildActivityPosition: 'bottom-right', // ẩn icon build ở góc trái dưới
    },
};

export default nextConfig;
